class Category {
    id: number
    type: string
    title: string

    constructor(id: number, type: string, title: string) {
        this.id = id
        this.type = type
        this.title = title
    }
}